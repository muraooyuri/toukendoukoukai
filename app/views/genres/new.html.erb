<%= form_with model: @touken, local: true do |form| %>
  <%= form.text_field :title %>
  <%= form.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name), include_blank: true %>
  <%= form.submit '作成' %>
<% end %>